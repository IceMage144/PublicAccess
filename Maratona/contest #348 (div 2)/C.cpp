#include <cstdio>

using namespace std;

//void shiftRight(int **mat, int lin){

//}

int main(){
	int lin, col, inst, top;
	scanf("%d%d%d", &lin, &col, &inst);
	top = inst;
	int matrix[lin][col];
	int pile[inst][4];
	for (int)
	
	return 0;
}