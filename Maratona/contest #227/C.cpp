#include <iostream>
#include <cstring>

using namespace std;

int main(){
	char concat[112345], aux1[112345], aux2[112345];
	cin >> concat;
	int size = strlen(concat), mark = 0, trash = 0;
	do {
		aux[mark] = concat[mark];
		mark++;
		trash++;
	} while (aux[mark] != "0" && mark < size);
	while (mark < size){
		while ()
	}
	return 0;
}